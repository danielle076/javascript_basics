// 1. Declare a variable and store the result of 4 plus 5 in it.
const add = 4 + 5;
console.log(add); // gives 9

// 2. Declare a variable and store the result of 7 times 52 in it.
const multiply = 7 * 52;
console.log(multiply); //gives 364

// 3. Declare a variable and store in it the result of 4 + 3 multiplied by 7.
const sum = (4 + 3) * 7;
console.log(sum); // gives 49

// 4. Declare a variable and store in it the result of 36 divided by 6, times 2.
const secondSum = (36 / 6) * 2;
console.log(secondSum); // gives 12

// 5. Declare two variables and put in them the values 'zoet' and 'sappig' respectively.
// Declare another variable and store in it the merge of the above variables.
const a = 'zoet';
const b = 'sappig';
const c = a + b;
console.log(c); // gives "zoetsappig"

// 6. Declare a variable for your first name and a variable for your last name and put in the values.
// Then declare a variable for your full name and paste the above variables together.
// Make sure there is a space in between without changing anything about the original variables.
const firstName = 'Daniëlle';
const lastName = 'van den Akker';
const fullName = firstName + ' ' + lastName;
console.log(fullName); // gives Daniëlle van den Akker

// 7. Declare a variable for your city and a variable for province and put the values in it.
// Then declare a variable for your location and paste your city and province together.
// Make sure there is a comma and space in between without changing anything about the original variables.
const city = 'Breda';
const state = 'Noord-Brabant';
const location = city + ', ' + state;
console.log(location); // gives Breda, Noord-Brabant