// Not assigned: undefined
// Numeric values like 4, 223902 and 0.75
// Textual values (strings) such as "Hello!", "I'm a string" and "Cookies are delicious"
// Boolean values, where the only options are true and false
// Empty value: null (this type is not really primitive, but for now we'll consider it as such)

const numberOfLamps = 4;
const isLightOn = false;
const question = "Heb ik het licht wel uitgedaan?";
let shoppingCart = null;
